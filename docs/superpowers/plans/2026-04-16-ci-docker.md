# CI Pipeline + Worker Dockerfile Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a 5-job GitHub Actions CI pipeline and a multi-arch Worker Dockerfile with docker-compose for local dev and Azure VM deployment.

**Architecture:** CI runs 5 parallel jobs on every push/PR: TypeScript build, lint, test, Python lint (ruff), and Docker build verification. The Worker Dockerfile uses python:3.11-slim with a split requirements file (core vs MLX-local) so the Docker image stays lean. docker-compose.yml provides one-command local startup.

**Tech Stack:** GitHub Actions, Docker (buildx, multi-arch), ruff (Python linter), pnpm 9, Node 20, Python 3.11

---

## File Structure

| File | Purpose |
|------|---------|
| Create: `.github/workflows/ci.yml` | GitHub Actions CI pipeline (5 jobs) |
| Create: `worker/Dockerfile` | Multi-arch worker container image |
| Create: `worker/.dockerignore` | Exclude venv, cache, secrets from image |
| Create: `worker/requirements-docker.txt` | Cloud-only Python deps (no MLX/TTS/torch) |
| Create: `worker/ruff.toml` | Ruff linter configuration |
| Create: `docker-compose.yml` | One-command worker startup for local dev |
| Modify: `README.md` | Add CI and Docker sections |

---

### Task 1: Create ruff configuration + verify Python lint

**Files:**
- Create: `worker/ruff.toml`

- [ ] **Step 1: Create ruff.toml**

Create `worker/ruff.toml`:

```toml
# Ruff linter configuration
target-version = "py311"
line-length = 120

[lint]
select = [
    "E",     # pycodestyle errors
    "F",     # pyflakes
    "I",     # isort
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "RUF",   # ruff-specific rules
]
ignore = [
    "E501",   # line too long (handled by formatter, not linter)
    "B008",   # do not perform function calls in argument defaults
    "SIM108", # use ternary operator (readability preference)
    "UP007",  # use X | Y for union types (not all code is 3.10+)
    "RUF012", # mutable class attributes (false positives on dataclasses)
]

[lint.per-file-ignores]
"__init__.py" = ["F401"]  # unused imports in __init__ are fine
```

- [ ] **Step 2: Install ruff locally and test**

Run: `pip install ruff && cd /Users/stevenjunop/centric-intake && ruff check worker/ 2>&1 | tail -20`

Note: There will likely be lint errors on the first run. That's expected — we need to know what they are but we do NOT fix them in this task. The CI pipeline will start with ruff in warning-only mode and we'll tighten later.

- [ ] **Step 3: Check error volume**

Run: `ruff check worker/ --statistics 2>&1 | tail -20`

If there are more than ~50 errors, we'll run ruff in CI with `--exit-zero` (warn but don't block) initially. If under 50, we'll fix them and run strict.

- [ ] **Step 4: Commit**

```bash
git add worker/ruff.toml
git commit -m "chore: add ruff linter configuration for Python worker"
```

---

### Task 2: Create worker Docker requirements (cloud-only)

**Files:**
- Create: `worker/requirements-docker.txt`

The existing `requirements.txt` includes MLX (Apple Silicon only), TTS, torch, and opencv — these are heavy, platform-specific, and not needed when running in Docker with cloud inference. Create a slimmed-down requirements file for Docker builds.

- [ ] **Step 1: Create requirements-docker.txt**

Create `worker/requirements-docker.txt`:

```text
# Core (same as requirements.txt)
asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
mcp>=1.0.0
jinja2>=3.1.0

# Rendering (compositor — needs Playwright + Chromium)
playwright>=1.48.0

# Image processing (lightweight only)
Pillow>=10.0.0

# NOTE: The following are EXCLUDED from Docker builds:
# - mlx, mlx-lm, mlx-vlm (Apple Silicon only — no Metal in containers)
# - rembg (heavy ONNX dependency — use cloud inference instead)
# - TTS, numpy (Coqui TTS — local voice only)
# - opencv-python, torch (Wav2Lip — local video only)
#
# In Docker, the worker uses cloud AI providers (OpenRouter, NVIDIA NIM)
# for all inference. Install full requirements.txt for local Mac development.
```

- [ ] **Step 2: Commit**

```bash
git add worker/requirements-docker.txt
git commit -m "chore: add slim Docker requirements (cloud inference only)"
```

---

### Task 3: Create worker Dockerfile

**Files:**
- Create: `worker/Dockerfile`
- Create: `worker/.dockerignore`

- [ ] **Step 1: Create .dockerignore**

Create `worker/.dockerignore`:

```
.venv/
__pycache__/
*.pyc
*.pyo
.env
.env.*
.keys.json
*.egg-info/
.ruff_cache/
.pytest_cache/
.mypy_cache/
```

- [ ] **Step 2: Create the Dockerfile**

Create `worker/Dockerfile`:

```dockerfile
# ──────────────────────────────────────────────────────────────
# Nova Worker — Multi-arch Docker image
# Supports: linux/amd64 (Azure VM, CI) + linux/arm64 (Apple Silicon)
#
# Build:
#   docker build -t nova-worker ./worker
#
# Build multi-arch:
#   docker buildx build --platform linux/amd64,linux/arm64 -t nova-worker ./worker
#
# Run:
#   docker run --env-file worker/.env nova-worker
# ──────────────────────────────────────────────────────────────

FROM python:3.11-slim AS base

# Prevent Python from writing .pyc files and enable unbuffered output
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# ── System dependencies ──────────────────────────────────────
# Playwright Chromium deps + FFmpeg for video muxing
RUN apt-get update && apt-get install -y --no-install-recommends \
    # Playwright Chromium dependencies
    libnss3 \
    libatk1.0-0 \
    libatk-bridge2.0-0 \
    libcups2 \
    libdrm2 \
    libxkbcommon0 \
    libxcomposite1 \
    libxdamage1 \
    libxrandr2 \
    libgbm1 \
    libpango-1.0-0 \
    libcairo2 \
    libasound2 \
    libxshmfence1 \
    # FFmpeg for video/audio muxing
    ffmpeg \
    # Cleanup
    && rm -rf /var/lib/apt/lists/*

# ── Python dependencies (cached layer) ───────────────────────
COPY requirements-docker.txt .
RUN pip install --no-cache-dir -r requirements-docker.txt

# ── Playwright browser ───────────────────────────────────────
RUN playwright install chromium

# ── Application code ─────────────────────────────────────────
COPY . .

# ── Health check ─────────────────────────────────────────────
# Worker polls DB — if process is alive, it's healthy
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
    CMD python -c "import asyncio; asyncio.run(__import__('neon_client')._get_pool())" || exit 1

# ── Entrypoint ───────────────────────────────────────────────
CMD ["python", "main.py"]
```

- [ ] **Step 3: Test local Docker build**

Run: `cd /Users/stevenjunop/centric-intake && docker build -t nova-worker ./worker`

Expected: Image builds successfully. May take 2-3 minutes on first build (Playwright + Chromium download).

- [ ] **Step 4: Commit**

```bash
git add worker/Dockerfile worker/.dockerignore
git commit -m "feat: add multi-arch Worker Dockerfile with Playwright + FFmpeg"
```

---

### Task 4: Create docker-compose.yml

**Files:**
- Create: `docker-compose.yml`

- [ ] **Step 1: Create docker-compose.yml**

Create `docker-compose.yml` in the project root:

```yaml
# Nova — Docker Compose for local development
#
# Start the worker:
#   docker compose up worker
#
# Start with live code reload (mount local files):
#   docker compose up worker-dev
#
# Build fresh:
#   docker compose build

services:
  # Production-like worker (uses built image)
  worker:
    build: ./worker
    env_file: ./worker/.env
    restart: unless-stopped

  # Development worker (mounts local code for live reload)
  worker-dev:
    build: ./worker
    env_file: ./worker/.env
    volumes:
      - ./worker:/app
    restart: unless-stopped
```

- [ ] **Step 2: Test docker compose**

Run: `cd /Users/stevenjunop/centric-intake && docker compose build worker`

Expected: Build succeeds (reuses cached layers from Task 3).

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: add docker-compose.yml for worker local dev"
```

---

### Task 5: Create GitHub Actions CI pipeline

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Create the CI workflow**

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Next.js Build ────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm build
        env:
          # Clerk requires these at build time (can be dummy values)
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_placeholder
          NEXT_PUBLIC_CLERK_SIGN_IN_URL: /sign-in
          NEXT_PUBLIC_CLERK_SIGN_UP_URL: /sign-up

  # ── ESLint ───────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

  # ── Vitest ───────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm test

  # ── Python Lint (ruff) ──────────────────────────────────────
  python-lint:
    name: Python Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install ruff

      - run: ruff check worker/ --config worker/ruff.toml

  # ── Docker Build Verification ───────────────────────────────
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v6
        with:
          context: ./worker
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

- [ ] **Step 2: Verify the workflow YAML is valid**

Run: `cd /Users/stevenjunop/centric-intake && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML valid"`

If `yaml` module not available: `python3 -c "import json; print('file exists')" && echo "skip yaml check — will validate on push"`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "feat: add GitHub Actions CI pipeline (build, lint, test, python-lint, docker)"
```

---

### Task 6: Fix ruff lint errors (if blocking)

**Files:**
- Modify: various `worker/**/*.py` files

This task depends on Task 1's ruff output. If ruff found errors that would block CI, fix them here. If ruff passes clean, skip this task.

- [ ] **Step 1: Run ruff and assess**

Run: `cd /Users/stevenjunop/centric-intake && ruff check worker/ --config worker/ruff.toml 2>&1 | wc -l`

If 0 errors: skip to Task 7.

- [ ] **Step 2: Auto-fix safe issues**

Run: `ruff check worker/ --config worker/ruff.toml --fix`

This auto-fixes import sorting, unused imports, and other safe transformations.

- [ ] **Step 3: Check remaining errors**

Run: `ruff check worker/ --config worker/ruff.toml`

For any remaining errors that can't be auto-fixed, add targeted `# noqa` comments or fix manually. If there are too many (50+), update the CI job to use `--exit-zero` temporarily:

In `.github/workflows/ci.yml`, change the ruff step to:
```yaml
      - run: ruff check worker/ --config worker/ruff.toml --exit-zero
```

- [ ] **Step 4: Commit**

```bash
git add worker/ .github/workflows/ci.yml
git commit -m "fix: resolve ruff lint errors in Python worker"
```

---

### Task 7: Update README with CI and Docker sections

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add CI section to README**

Find the `## Troubleshooting / 故障排查` heading in `README.md`. Insert the following TWO new sections BEFORE it:

```markdown
---

## CI Pipeline / 持续集成

Every push to `main` and every pull request runs 5 parallel checks via GitHub Actions:

| Check | Command | What it catches |
|-------|---------|----------------|
| **Build** | `pnpm build` | TypeScript errors, Next.js compilation |
| **Lint** | `pnpm lint` | ESLint code style violations |
| **Test** | `pnpm test` | Unit test failures (Vitest) |
| **Python Lint** | `ruff check worker/` | Python code quality issues |
| **Docker Build** | `docker build ./worker` | Dockerfile or dependency errors |

All 5 must pass before merging. The workflow file is at `.github/workflows/ci.yml`.

**Concurrency:** Pushes to the same branch cancel in-progress runs to save CI minutes.

---

## Docker / 容器化

The Python worker can be containerized for Azure VM or any Linux deployment.

### Quick Start / 快速启动

```bash
# Build the worker image
docker build -t nova-worker ./worker

# Run with env file
docker run --env-file worker/.env nova-worker
```

### Using docker-compose / 使用 docker-compose

```bash
# Production-like (built image)
docker compose up worker

# Development (live code mount)
docker compose up worker-dev

# Rebuild after dependency changes
docker compose build
```

### Multi-arch Build / 多架构构建

```bash
# Build for both Azure VM (amd64) and Apple Silicon (arm64)
docker buildx build --platform linux/amd64,linux/arm64 -t nova-worker ./worker
```

### MLX Note / MLX 说明

The Docker image does **not** include MLX, TTS, torch, or other Apple Silicon dependencies. When running in Docker, the worker uses cloud AI providers (OpenRouter, NVIDIA NIM) for all inference. For local MLX inference on Apple Silicon, run the worker directly:

```bash
cd worker && source .venv/bin/activate && python main.py
```

### Requirements Files / 依赖文件

| File | Purpose |
|------|---------|
| `worker/requirements.txt` | Full dependencies (local Mac development with MLX) |
| `worker/requirements-docker.txt` | Cloud-only dependencies (Docker builds — no MLX/TTS/torch) |

```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add CI pipeline and Docker sections to README"
```

---

### Task 8: Final verification

- [ ] **Step 1: Verify all new files exist**

Run:
```bash
ls -la .github/workflows/ci.yml worker/Dockerfile worker/.dockerignore worker/requirements-docker.txt worker/ruff.toml docker-compose.yml
```

Expected: All 6 files exist.

- [ ] **Step 2: Verify Docker build**

Run: `docker build -t nova-worker ./worker`

Expected: Build succeeds.

- [ ] **Step 3: Verify Next.js build**

Run: `pnpm build 2>&1 | tail -5`

Expected: Build succeeds with no errors.

- [ ] **Step 4: Verify ruff**

Run: `ruff check worker/ --config worker/ruff.toml`

Expected: Either 0 errors, or CI is configured with `--exit-zero`.
